<ul class="nav nav-pills justify-content-center mb-3 market-action-tab-ul" id="pills-tab" role="tablist">
  <li class="nav-item" role="presentation">
    <a class="nav-link active" id="pills-all-mcx-tab" data-bs-toggle="pill" href="#pills-all-mcx" role="tab"
      aria-controls="pills-all-mcx" aria-selected="true">All</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="pills-gainers-mcx-tab" data-bs-toggle="pill" href="#pills-gainers-mcx" role="tab"
      aria-controls="pills-gainers-mcx" aria-selected="false">Top gainer</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="pills-losers-mcx-tab" data-bs-toggle="pill" href="#pills-losers-mcx" role="tab"
      aria-controls="pills-losers-mcx" aria-selected="false">Top losers</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="pills-open-low-mcx-tab" data-bs-toggle="pill" href="#pills-open-low-mcx" role="tab"
      aria-controls="pills-open-low-mcx" aria-selected="false">Same open & low</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="pills-open-high-mcx-tab" data-bs-toggle="pill" href="#pills-open-high-mcx" role="tab"
      aria-controls="pills-open-high-mcx" aria-selected="false">Same open & high</a>
  </li>
</ul>

<div class="tab-content">
  <div class="tab-pane fade show active" id="pills-all-mcx" role="tabpanel" aria-labelledby="pills-all-mcx-tab">
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr> 
            <% columns = ['symbol', 'open_price', 'high_price', 'low_price', 'last_price', 'prev_price', 'per_change', 'total_traded_volume', 'total_traded_value'] %>
            <% columns.each do |column| %>
              <th scope="col"><%= column.humanize %></th>
            <% end %>
            <th scope="col">Favorite</th>
          </tr>
        </thead>
        <tbody>
          <%= display_stock_data(@stock_mcx, columns, true) %>
        </tbody>
      </table>
    </div>
</div>

  <div class="tab-pane fade" id="pills-gainers-mcx" role="tabpanel" aria-labelledby="pills-gainers-mcx-tab">
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr> 
            <% columns = ['symbol', 'open_price', 'high_price', 'low_price', 'last_price', 'prev_price', 'per_change'] %>
            <% columns.each do |column| %>
              <th scope="col"><%= column.humanize %></th>
            <% end %>
            <th scope="col">Favorite</th>
          </tr>
        </thead>
        <tbody>
          <%= display_stock_data(@gainers_mcx, columns, true) %>
        </tbody>
      </table>
    </div>
  </div>
  
  <div class="tab-pane fade" id="pills-losers-mcx" role="tabpanel" aria-labelledby="pills-losers-mcx-tab">
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <% columns = ['symbol', 'open_price', 'high_price', 'low_price', 'last_price', 'prev_price', 'per_change'] %>
            <% columns.each do |column| %>
              <th scope="col"><%= column.humanize %></th>
            <% end %>
            <th scope="col">Favorite</th>
          </tr>
        </thead>
        <tbody>
          <%= display_stock_data(@losers_mcx, columns, true) %>
        </tbody>
      </table>
    </div>
  </div>
  <div class="tab-pane fade" id="pills-open-low-mcx" role="tabpanel" aria-labelledby="pills-open-low-mcx-tab">
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <% columns = ['symbol', 'open_price', 'high_price', 'low_price', 'last_price', 'prev_price', 'per_change'] %>
            <% columns.each do |column| %>
              <th scope="col"><%= column.humanize %></th>
            <% end %>
            <th scope="col">Favorite</th>
          </tr>
        </thead>
        <tbody>
          <%= display_stock_data(@same_open_low_mcx,columns, true) %>
        </tbody>
      </table>
    </div>
  </div>

  <div class="tab-pane fade" id="pills-open-high-mcx" role="tabpanel" aria-labelledby="pills-open-high-mcx-tab">
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <% columns = ['symbol', 'open_price', 'high_price', 'low_price', 'last_price', 'prev_price', 'per_change'] %>
            <% columns.each do |column| %>
              <th scope="col"><%= column.humanize %></th>
            <% end %>
            <th scope="col">Favorite</th>
          </tr>
        </thead>
        <tbody>
          <%= display_stock_data(@same_open_high_mcx, columns, true) %>
        </tbody>
      </table>
    </div>
  </div>
</div>
